<%= form_for(@song) do |s| %>
  <%= s.text_field :title, @song.title%>
  <%= s.check_box :released, @song.released%>
  <%= s.text_field :release_year, @song.release_year%>
  <%= s.text_field :artist_name, @song.artist_name%>
  <%= s.text_field :genre, @song.genre%>
  <%= s.submit %>
<% end %>
